services:
  - type: web
    name: django-backend
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn DOCnDOSE.wsgi:application"
    envVars:
      - key: DATABASE_URL
        value: sqlite:///db.sqlite3
      - key: SECRET_KEY
        generateValue: true
